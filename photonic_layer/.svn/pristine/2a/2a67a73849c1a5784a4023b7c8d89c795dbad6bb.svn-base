package Simulations.gds;

import GDS.Headers.Header;
import ch.epfl.general_libraries.clazzes.ParamName;

public class TestHeader {
	
	Header hd ;
	String fileName ;
	
	public TestHeader(
			@ParamName(name="File Name (no .py extension)") String fileName,
			@ParamName(name="Header lines") Header hd
			){
		this.hd = hd ;
		this.fileName = fileName ;
	}
	
	
//	@Override
//	public void run(AbstractResultsManager man, AbstractResultsDisplayer dis) throws WrongExperimentException {
//		FileOutput fout = new FileOutput(fileName + ".py","w") ;
//		fout.println(hd.getPythonCode(fileName));
//		fout.close();
//		
//	}
	
	
}
